#.travis.yml

language: node_js
node_js:
  - "10.16.0"
dist: trusty
sudo: required
branches:
  only:
  - master
before_script:
  - npm install -g @angular/cli
  deploy:
  provider: firebase
  token:
    secure: $FIREBASE_TOKEN
  project: "reliable-strata-239605"
   message: "PROBANDO"
script:
- ng lint
- ng build --prod --base-href https://github.com/Tortu/Seminario